# Workflow: Content Publishing

Applies to Events, Announcements and Sermons.

Draft → validate required fields → preview → publish → verify public read → record audit/evidence.

Unpublish/delete are consequential operations and require explicit human intent.

A publish button is not green until the resulting public state is verified from the real backend.
